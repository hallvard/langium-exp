specification ngu.nadag

import ngu.common

type GU {
  # lokalId: Identifikasjon
  @ "område": Areal
  borehull*: type GB {
    # lokalId: Identifikasjon
    @ posisjon: Posisjon
    "undersøkelser"*: type GBU {
      # lokalId: Identifikasjon
      @ posisjon: Posisjon
    }
  }
}
