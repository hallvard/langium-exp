specification ngu.nadag

import ngu.common

type GU {
  @ "område": Areal
  # lokalId: Identifikasjon
  borehull*: type GB {
    @ posisjon: Posisjon
    # lokalId: Identifikasjon
    "undersøkelser"*: type GBU {
      # lokalId: Identifikasjon
      @ posisjon: Posisjon
    }
  }
}
